import * as chatController from './chatController.js';

export const sendChatMessage = chatController.sendChatMessage;
export const getChatHealth = chatController.getChatHealth;
export const getChatHistory = chatController.getChatHistory;

export default chatController.default;
