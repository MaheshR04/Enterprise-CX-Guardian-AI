{
  "name": "guardianpath-ai",
  "version": "1.0.0",
  "private": true,
  "description": "GuardianPath AI - real-time safety navigation and emergency response MVP.",
  "workspaces": [
    "frontend",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace frontend\"",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace server",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace server",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
